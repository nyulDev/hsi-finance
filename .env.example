# Copy to .env.local (or .env) and fill in your database URL
# For production, prefer a real .env.local and add it to .gitignore

DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require

